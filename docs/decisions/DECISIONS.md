# Human decisions (spec 24.2)

These decisions are deliberately left to humans. Every row was decided by the programme owner on 24–25 September
2026; the decision replaces the earlier working assumption.

| ID   | Decision                                                             | Status                                           | Working assumption (labelled, reversible)                                                                                                                                                                                                                                                                                               | What changes when decided                                                                                                   |
| ---- | -------------------------------------------------------------------- | ------------------------------------------------ | --------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | --------------------------------------------------------------------------------------------------------------------------- |
| D-01 | MySQL 8 or TiDB, and which managed service                           | **Decided (programme owner, 25 September 2026)** | MySQL 8 on Railway's managed MySQL. TiDB only if write volume outgrows a single primary; the TiDB hotspot notes stay documented, not applied.                                                                                                                                                                                           | None now; revisit at scale                                                                                                  |
| D-02 | Hosting provider and data regions                                    | **Decided (programme owner, 25 September 2026)** | Railway. Target region: EU West (Amsterdam), closest Railway region to southern Africa; moved while data is small, after backup and restore are verified. `tenants.data_region = 'default'`.                                                                                                                                            | Region move (runbook), object-store location in the same region                                                             |
| D-03 | Authentication provider                                              | **Decided (programme owner, 24 September 2026)** | Google authenticates through OpenID Connect (`openid-client` 6, authorization code + PKCE + state + nonce, ID token signature checked against Google's JWKS); Oremedia's policy layer alone authorises. No self-sign-up: an existing identity, an existing user by verified email, or an invitation; otherwise refused. MFA: see below. | Done: `apps/api/src/auth`, access module sign-in, migration 0003 (`external_identities`, `auth_events`), runbook section 1a |
| D-04 | Release 1 channel set                                                | **Decided (programme owner, 25 September 2026)** | Release 1: Facebook Page, Instagram Business (one Meta developer app and review) and LinkedIn Page. X and TikTok move to Release 2.                                                                                                                                                                                                     | Certification per channel once the developer apps are approved                                                              |
| D-05 | Polotno licence enquiry vs Konva-only                                | **Decided (programme owner, 25 September 2026)** | Konva only; no Polotno licence enquiry.                                                                                                                                                                                                                                                                                                 | None                                                                                                                        |
| D-06 | Image-generation provider and data terms                             | **Decided (programme owner, 25 September 2026)** | All generative AI goes through OpenRouter (ADR-11): images with Nano Banana / Nano Banana 2, video with Seedance 2.x (others allowed by policy), speech and music through OpenRouter's audio models. OreMedia's routing policy chooses the model per task; generated media enters the asset pipeline as AI-generated with provenance.   | ADR-11 adapters, media-generation workflows, per-brand provider allowlist                                                   |
| D-07 | ADR-06 licensing route for any reference-codebase code               | **Decided (programme owner, 25 September 2026)** | Clean-room only; the AGPL route is not taken and no reference-codebase code is copied.                                                                                                                                                                                                                                                  | None                                                                                                                        |
| D-08 | Plans, prices, entitlements, downgrade and billing-failure behaviour | **Decided (programme owner, 25 September 2026)** | Free `pilot` plan for the pilot, generous limits, 14-day grace (spec 22.1). Commercial pricing decided before general release.                                                                                                                                                                                                          | Plan table and billing integration at general release                                                                       |
| D-09 | Retention periods per data class                                     | **Decided (programme owner, 25 September 2026)** | Spec 17.5 defaults, as seeded in `retention_policies`.                                                                                                                                                                                                                                                                                  | None                                                                                                                        |
| D-10 | SLOs, RPO/RTO                                                        | **Decided (programme owner, 25 September 2026)** | Targets: RPO 15 min, RTO 4 h, spec 17.2 SLOs. The RPO is conditional on verifying what Railway MySQL backups provide; the gap is recorded if they cannot meet it.                                                                                                                                                                       | Backup verification, alert thresholds                                                                                       |
| D-11 | Separation of duties on by default                                   | **Decided (programme owner, 25 September 2026)** | On by default for client brands (`require_distinct_approver = true`: the approver cannot be the author); off by default for internal brands.                                                                                                                                                                                            | Brand policy default by brand kind                                                                                          |
| D-12 | Oremedia product identity                                            | **Decided (programme owner, 25 September 2026)** | The product keeps its own neutral brand in code, replaceable later. Each deployment loads its own brand at deploy time: this deployment is Ore & Tar; a PointFive Group deployment is a separate Railway project built from the same repository.                                                                                        | Deploy-time brand configuration; Ore & Tar tokens                                                                           |

## Recorded specification interpretations

| Ref       | Interpretation                                                                                                                                                                                                                                                                                                                                               | Status                                            |
| --------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------ | ------------------------------------------------- |
| Spec 17.6 | After a restore, `scheduled` and unsent `dispatching` publications move to `held` (`restored_from_backup`); `processing` and sent `dispatching` publications move to `outcome_unknown` and are reconciled against remote history. Reason: 13.1's exhaustive table has no `held → published` move, and 14.3 sends an attempt with `sentAt` to reconciliation. | **Accepted (programme owner, 24 September 2026)** |

### D-03 consequence: second factor

Google does not reliably report second-factor use in ID token claims (`amr` is not part of Google's documented
claims), so Oremedia does not claim MFA for Google sign-ins: `users.mfa_enrolled` stays `false` for every user.
Consequence: a brand policy with `mfaRequired: true` (policy document v1) denies every Google-authenticated user
with `mfa_required` wherever that policy is consulted (review decisions today), so it must stay off. The
recommended control is outside Oremedia: enforce 2-Step Verification in the Google Workspace admin console for the
users of Oremedia, and set `AUTH_ALLOWED_DOMAINS` so only those Workspace accounts can sign in (runbook
deploy-railway.md section 1a). Revisit if Oremedia adds its own second factor or Google documents a reliable claim.

### D-03 follow-up: invitations are accepted on sign-in

**Decided (programme owner, 25 September 2026): keep the current behaviour.** Signing in with the invited, verified
Google account is the acceptance. The analysis that informed the decision follows. Behaviour: when a person signs in with Google, every pending invitation addressed to
their verified email is accepted automatically; there is no separate "accept" step. Consequence: an owner who knows
a user's email can add that user to their company, and the company then appears in that user's portfolio without
the user having agreed (the user gains access, never loses any; the inviting company learns nothing more than it
already knew, the email). Alternative: explicit accept — invitations show as pending in the portfolio and a
membership becomes active only when the person accepts (and can decline). Changing it touches the sign-in
resolution in `packages/modules/access/src/service.ts` and adds an accept/decline procedure and portfolio state.

## How to record a decision

Edit the row: set Status to **Decided (owner, date)**, replace the working assumption with the decision, and open a
follow-up issue for "What changes when decided".
