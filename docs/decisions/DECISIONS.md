# Human decisions (spec 24.2)

These decisions are deliberately left to humans. Each row states the working assumption used to keep the build
moving, labelled as such. Nothing below is decided until a named owner records it here.

| ID   | Decision                                                             | Status                                           | Working assumption (labelled, reversible)                                                                                                                                                                                                                                                                                               | What changes when decided                                                                                                   |
| ---- | -------------------------------------------------------------------- | ------------------------------------------------ | --------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | --------------------------------------------------------------------------------------------------------------------------- |
| D-01 | MySQL 8 or TiDB, and which managed service                           | **Open**                                         | MySQL 8.0 (Railway MySQL plugin in hosting; local MySQL 8.0.46 for tests). TiDB hotspot handling documented, not applied.                                                                                                                                                                                                               | Migration SQL for high-write tables; ID strategy; FK verification job                                                       |
| D-02 | Hosting provider and data regions                                    | **Open** (user directed Railway for hosting)     | Railway, single region as configured by the user; `tenants.data_region = 'default'`. Residency obligations unknown.                                                                                                                                                                                                                     | Region pinning, object-store location                                                                                       |
| D-03 | Authentication provider                                              | **Decided (programme owner, 24 September 2026)** | Google authenticates through OpenID Connect (`openid-client` 6, authorization code + PKCE + state + nonce, ID token signature checked against Google's JWKS); Oremedia's policy layer alone authorises. No self-sign-up: an existing identity, an existing user by verified email, or an invitation; otherwise refused. MFA: see below. | Done: `apps/api/src/auth`, access module sign-in, migration 0003 (`external_identities`, `auth_events`), runbook section 1a |
| D-04 | Release 1 channel set                                                | **Open**                                         | Adapter scaffolds for LinkedIn Page, Instagram Business, Facebook Page, X and TikTok, none certified (`certifiedAt = null`).                                                                                                                                                                                                            | Certification work per channel with a real developer app                                                                    |
| D-05 | Polotno licence enquiry vs Konva-only                                | **Open**                                         | Konva only.                                                                                                                                                                                                                                                                                                                             | Only with written confirmation, behind `EditorAdapter`                                                                      |
| D-06 | Image-generation provider and data terms                             | **Open**                                         | `ImageGenerationAdapter` interface only; no provider wired.                                                                                                                                                                                                                                                                             | Adapter implementation, cost table                                                                                          |
| D-07 | ADR-06 licensing route for any reference-codebase code               | **Open**                                         | No code copied.                                                                                                                                                                                                                                                                                                                         | Nothing unless AGPL route accepted                                                                                          |
| D-08 | Plans, prices, entitlements, downgrade and billing-failure behaviour | **Open**                                         | Placeholder plan `pilot` with generous limits; grace 14 days as in spec 22.1.                                                                                                                                                                                                                                                           | Plan table seed, Stripe or other billing integration                                                                        |
| D-09 | Retention periods per data class                                     | **Open**                                         | Spec 17.5 defaults seeded into `retention_policies`.                                                                                                                                                                                                                                                                                    | TTL jobs' parameters                                                                                                        |
| D-10 | SLOs, RPO/RTO                                                        | **Open**                                         | Spec 17.2 placeholders; RPO 15 min / RTO 4 h draft.                                                                                                                                                                                                                                                                                     | Alert thresholds, backup cadence                                                                                            |
| D-11 | Separation of duties on by default                                   | **Open**                                         | Off (`require_distinct_approver = false`) per brand policy.                                                                                                                                                                                                                                                                             | Policy version default                                                                                                      |
| D-12 | Oremedia product identity                                            | **Open**                                         | Neutral placeholder tokens in `packages/ui/src/tokens.css`.                                                                                                                                                                                                                                                                             | Load the governing brand skill's tokens before Phase 3 UI polish                                                            |

## Recorded specification interpretations

| Ref       | Interpretation                                                                                                                                                                                                                                                                                                                                               | Status                                            |
| --------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------ | ------------------------------------------------- |
| Spec 17.6 | After a restore, `scheduled` and unsent `dispatching` publications move to `held` (`restored_from_backup`); `processing` and sent `dispatching` publications move to `outcome_unknown` and are reconciled against remote history. Reason: 13.1's exhaustive table has no `held → published` move, and 14.3 sends an attempt with `sentAt` to reconciliation. | **Accepted (programme owner, 24 September 2026)** |

### D-03 consequence: second factor

Google does not reliably report second-factor use in ID token claims (`amr` is not part of Google's documented
claims), so Oremedia does not claim MFA for Google sign-ins: `users.mfa_enrolled` stays `false` for every user.
Consequence: a brand policy with `mfaRequired: true` (policy document v1) denies every Google-authenticated user
with `mfa_required` wherever that policy is consulted (review decisions today), so it must stay off. The
recommended control is outside Oremedia: enforce 2-Step Verification in the Google Workspace admin console for the
users of Oremedia, and set `AUTH_ALLOWED_DOMAINS` so only those Workspace accounts can sign in (runbook
deploy-railway.md section 1a). Revisit if Oremedia adds its own second factor or Google documents a reliable claim.

### D-03 open item: invitations are accepted on sign-in (product owner to decide)

**Open.** Current behaviour (unchanged): when a person signs in with Google, every pending invitation addressed to
their verified email is accepted automatically; there is no separate "accept" step. Consequence: an owner who knows
a user's email can add that user to their company, and the company then appears in that user's portfolio without
the user having agreed (the user gains access, never loses any; the inviting company learns nothing more than it
already knew, the email). Alternative: explicit accept — invitations show as pending in the portfolio and a
membership becomes active only when the person accepts (and can decline). Changing it touches the sign-in
resolution in `packages/modules/access/src/service.ts` and adds an accept/decline procedure and portfolio state.

## How to record a decision

Edit the row: set Status to **Decided (owner, date)**, replace the working assumption with the decision, and open a
follow-up issue for "What changes when decided".
