import { PerformanceScreen } from '../../../../../../features/measurement/performance-screen';

/** `performance`: published posts in a period with their measured numbers; the feature slice owns the screen. */
export function PerformanceRoute() {
  return <PerformanceScreen />;
}
